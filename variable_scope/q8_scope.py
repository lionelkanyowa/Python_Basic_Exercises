# Q.8:

print(greeting)

greeting = 'Hello word!'

# The code above would produce a NameError. This is because the print(greeting) statement is invoked on line 3 before
# the greeting variable was initialized. The name 'greeting' needs to be defined first.
